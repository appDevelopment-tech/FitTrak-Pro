# Руководство по авторизации в FitTrak-Pro

## Разделение доступа

Система имеет **раздельные входы** для разных типов пользователей:

### 🔐 Для учеников
- **URL:** `/login`
- **Доступ:** Публичный
- **Функции:** Регистрация, вход, просмотр расписания

### 🛡️ Для тренеров/админов
- **URL:** `/admin/login` (скрытый)
- **Доступ:** Только для персонала
- **Функции:** Управление системой, учениками, расписанием

## Как войти в систему

### 🎯 Текущий режим (тестовый)
В настоящее время включен режим обхода авторизации (`BYPASS_AUTH = true`).

**Для входа:**
1. Перейдите на страницу `/login` или `/admin/login`
2. Введите любой email и пароль
3. Система автоматически войдет как тестовый тренер

### 🔐 Режим реальной авторизации

#### Для тренеров/админов:
**Данные для входа:**
- **Админ:** `admin@fittrak.pro` / `admin123`
- **Тренер:** `trainer@fittrak.pro` / `trainer123`

**Что можно использовать для входа:**
- Email: `admin@fittrak.pro` или `trainer@fittrak.pro`
- Имя пользователя: `admin` или `trainer1`
- Пароль: соответствующий пароль

#### Для учеников:
**Данные для входа:**
- **Ученик 1:** `student1@fittrak.pro` / `student123`
- **Ученик 2:** `student2@fittrak.pro` / `student123`
- **Несовершеннолетняя:** `student3@fittrak.pro` / `student123`

**Что можно использовать для входа:**
- Email: `student1@fittrak.pro`, `student2@fittrak.pro`, `student3@fittrak.pro`
- Телефон: `+7 (999) 111-11-11`, `+7 (999) 222-22-22`, `+7 (999) 333-33-33`
- Пароль: `student123`

## Процесс входа

### 1. Выбор правильного входа
- **Ученики:** Используйте `/login`
- **Тренеры/Админы:** Используйте `/admin/login`

### 2. Ввод данных
- **Для тренеров:** Email или имя пользователя + пароль
- **Для учеников:** Email или телефон + пароль

### 3. Автоматическая проверка роли
Система автоматически проверяет права доступа:
- Ученики не могут войти через админский вход
- Тренеры не могут войти через публичный вход

## Создание тестовых пользователей

Для создания тестовых пользователей выполните миграцию:

```sql
-- Миграция создаст:
-- 2 тренера (admin, trainer1)
-- 3 ученика (включая несовершеннолетнюю)
```

## Отключение тестового режима

Чтобы включить реальную авторизацию:

1. Откройте `client/src/lib/auth.tsx`
2. Измените `BYPASS_AUTH = false`
3. Обновите логику авторизации для работы с API

## Безопасность

⚠️ **Важно:** В текущей реализации пароли хранятся в открытом виде. Для продакшена необходимо:
- Хеширование паролей (bcrypt)
- JWT токены для сессий
- HTTPS для передачи данных
- Валидация и санитизация входных данных

## Структура данных

### Таблица `users` (тренеры)
```sql
- id: serial primary key
- username: text unique
- password: text
- first_name: text
- last_name: text
- email: text unique
- phone: text
- is_trainer: boolean default true
```

### Таблица `students` (ученики)
```sql
- id: serial primary key
- first_name: text
- last_name: text
- email: text unique
- phone: text
- password: text
- birth_date: text
- status: text ('pending', 'active', 'inactive')
- ...другие поля
```

## Маршрутизация и защита

### Публичные маршруты
- `/` - Главная страница с публичным расписанием
- `/login` - Вход для учеников

### Защищенные маршруты для учеников
- `/dashboard` - Личный кабинет ученика
- `/cabinet` - Альтернативный путь к кабинету

### Защищенные маршруты для тренеров/админов
- `/admin/login` - Скрытый вход для персонала
- `/admin/dashboard` - Панель администратора

### Автоматические редиректы
- Ученики, пытающиеся войти в админку → `/login`
- Тренеры, пытающиеся войти в публичную часть → `/admin/login`
- Неавторизованные пользователи → соответствующий вход

## API эндпоинты

### Авторизация
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/register` - регистрация ученика
- `POST /api/auth/forgot-password` - восстановление пароля

### Параметры запроса
```json
{
  "emailOrPhone": "user@example.com",
  "password": "password123",
  "userType": "trainer" // или "pupil"
}
```

### Ответ при успешной авторизации
```json
{
  "user": { ... }, // для тренеров
  "pupil": { ... }, // для учеников
  "userType": "trainer" // или "pupil"
}
```

## Роли и права доступа

### 🛡️ АДМИН/ТРЕНЕР (СОЗДАТЕЛЬ)
**Полный доступ ко всему функционалу:**
- ✅ Управление всеми пользователями
- ✅ Управление расписанием
- ✅ Настройки системы
- ✅ Просмотр всех данных
- ✅ Создание, редактирование, удаление записей
- ✅ Управление учениками

### 👤 УЧЕНИК
**Доступ после авторизации:**
- ✅ Просмотр расписания
- ✅ Запись на тренировки
- ✅ Отмена записи
- ✅ Редактирование своего профиля
- ✅ Удаление своего профиля

**Ограничения:**
- ❌ Видит только свои записи в расписании
- ❌ Не имеет доступа к данным других учеников
- ❌ Не имеет админских прав
- ❌ Не может управлять системой

## Безопасность

### Разделение доступа
- ✅ Разные URL для разных ролей
- ✅ Скрытый админский вход
- ✅ Автоматическая проверка ролей
- ✅ Редиректы при неправильном доступе
- ✅ Изоляция данных между пользователями

### Защита маршрутов
- `RoleGuard` - компонент для проверки ролей
- `PupilRoute` - маршруты только для учеников
- `TrainerRoute` - маршруты только для тренеров
- `useRoleCheck` - хук для проверки роли в компонентах
- `PermissionGuard` - компонент для проверки прав доступа
- `AdminGuard` - компонент для админов/тренеров
- `PupilGuard` - компонент для учеников

### Система прав
- `usePermissions` - хук для работы с правами доступа
- `PERMISSIONS` - константы с описанием всех прав
- `hasPermission()` - проверка конкретного права
- `canViewOtherUsersData()` - проверка доступа к чужим данным
- `canManageSystem()` - проверка прав управления системой
